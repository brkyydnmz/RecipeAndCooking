import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    backgroundColor: '#FFA500', // koyu sarı renk
  },
  listContainer: {
    // Bu stil'i ekledim
    padding: 10, // Üstten biraz boşluk eklemek için
  },
});

export default styles;
